ll unsigned int binary_exp_mod_n(ll int a,ll int b,int p)
{
    ll unsigned res = 1;
    while(b)
    {
        if(b&1)
        res = res*a%p;
        a = a*a%p
        b>>=1;
    }
    return res;
}